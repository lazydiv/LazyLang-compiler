math.lazy > lexer > parser > explain > explain
```lazy
if a > y  {
 lazyPrint(a)
}
```
how does the lexer work on this
toknize the code into tokens
['if', 'a', '>', 'y', '{', 'lazyPrint', '(', 'a', ')', '}']


